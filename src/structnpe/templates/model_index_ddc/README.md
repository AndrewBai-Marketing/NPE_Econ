# Tiny Model-Index DDC Template

This smoke example targets a posterior over a finite mechanism index and a
finite parameter grid. It illustrates the mechanism-uncertainty interface; it
is not the paper's exact benchmark.
